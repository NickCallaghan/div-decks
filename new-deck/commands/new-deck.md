---
description: Generate a new div.deck-compatible slide deck as a self-contained HTML page
---

Load the new-deck skill, then generate a slide deck for: $@

Read the reference template at `./templates/slide-deck.html` and slide types at `./references/slide-types.md` before generating. Also read `./references/css-core.md` for required CSS and `./references/slide-engine.md` for the JavaScript engine.

If a branding skill is available, apply it AFTER generating the structural HTML. See `./references/branding.md`.

Write to the project's presentations directory and open in the browser.
